- type: replace
  path: /instance_groups/name=ha_proxy/instances
  value: 0

- type: replace
  path: /instance_groups/name=postgres/networks
  value:
    - name: ((default_network))
      static_ips:
      - ((postgres_sql_ip))
